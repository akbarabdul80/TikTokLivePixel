# Filter by size
def by_size(words, size):
    result = []
    for word in words:
        if len(word) == size:
            print(word)
            result.append(word)
    return result


def by_size(words, size):
    result = []
    for word in words:
        if len(word) == size:
            print(word)
            result.append(word)
    return result
